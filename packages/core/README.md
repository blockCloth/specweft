# @specweft/core

Core project profiling, MCP/Skill pool management, runtime assembly, review,
and memory logic for SpecWeft.

Most users should install the CLI package:

```bash
npm install -g specweft
```

See the repository README for full usage.
