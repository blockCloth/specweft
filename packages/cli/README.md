# SpecWeft CLI

Command line interface and MCP server for SpecWeft.

Install:

```bash
npm install -g specweft
```

Use inside a project:

```bash
specweft init
specweft start
specweft mcp-inspect
```

See the repository README for full usage.
